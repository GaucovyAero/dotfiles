#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

# only if you use ratpoison/StumpWM
export _JAVA_AWT_WM_NONREPARENTING=1

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
startx >> /dev/null 2>&1
fi
